# Same origin apps/web serves the API and web pages from. Must match one of
# manifest.json's host_permissions exactly, or the extension's fetch calls
# get blocked (MV3 uses host_permissions in place of CORS headers for
# privileged extension contexts).
VITE_API_BASE_URL=http://localhost:3000
