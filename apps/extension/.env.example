# Same values as apps/web/.env.local's NEXT_PUBLIC_SUPABASE_* — the anon key
# is safe to duplicate here (RLS protects the data behind it).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_API_BASE_URL=https://prometheus-azaroth.vercel.app
